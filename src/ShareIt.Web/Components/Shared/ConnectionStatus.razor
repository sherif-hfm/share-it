<div id="components-reconnect-modal" class="reconnect-banner" role="status" aria-live="polite">
    <span class="reconnect-wait">Connection interrupted. Reconnecting… Your unsaved text stays on this page.</span>
    <span class="reconnect-failed">Connection lost. Copy any unsaved text, then <button data-reload type="button">reload</button> to reconnect.</span>
</div>
